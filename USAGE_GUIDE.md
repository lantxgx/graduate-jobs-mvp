# 应届生招聘雷达使用说明

## 页面上的三个页面

### 1. 找岗位 `/`

这是用户的主工作台。可以不填写画像直接搜索全部活动岗位，使用公司、城市、职能、岗位族、招聘类型和学历筛选。点击“加载更多岗位”可以继续访问完整结果；推荐只改变排序和解释，不隐藏全部岗位。

### 2. 企业招聘入口注册表 `/admin/sources`

这是采集侧的后台信息，不是岗位列表。它回答的是：

- 企业的招聘官网入口在哪里；
- 使用飞书、北森、Moka 还是自建系统；
- 来源是否确认官方、是否可访问、质量等级和接入优先级是什么。

普通用户不需要在这里操作。只有进入采集队列并通过质量检查的来源，才会产生下面的具体岗位。

### 3. 能力画像与求职意愿 `/profile`

这是用户侧的个性化设置，而且分成两类：

- 能力证据：学历、毕业年份、专业、技能。可以由简历辅助提取，但必须由用户检查确认；
- 求职意愿：主攻岗位、可接受相邻岗位、目标企业、目标城市、明确排除的岗位。这些不能由简历自动推断，必须自己填写。

不填写画像也可以使用全部岗位搜索。

具体岗位工作台已独立到首页；公司名称来自具体岗位本身。用户可以查看地点/招聘类型/描述/要求，收藏、忽略、标记已投递，并跳转企业官方投递链接。

## 简历预览怎么用

1. 选择 PDF 或 DOCX 文件；
2. 点击“解析简历预览”；
3. 页面显示当前请求提取到的文本片段和候选字段；
4. 预览不会保存原始简历，也不会修改画像。

预览适合先检查文件是否能正常读取、中文是否乱码、教育和技能是否被识别。

## AI 结构化分析有什么用

它的作用是减少手动整理简历的工作，提取教育背景、毕业年份候选值、技能和不确定项，供用户确认后写入“能力证据”。

它不会：

- 自动决定你想投什么岗位；
- 自动添加目标企业或排除岗位；
- 自动过滤全部岗位；
- 给出录取概率；
- 自动投递。

当前默认使用本地规则分析，不会把简历发送到外部模型。只有配置外部兼容接口并勾选同意后，才会调用外部 AI；缺少配置时会失败关闭。

推荐操作顺序：先预览 → 检查结果 → AI 分析（可选）→ 点击“采用已分析能力画像条目” → 手动补充求职意愿 → 保存画像 → 点击“按我的画像推荐”。
