# Luna 可直接执行的岗位采集落地任务书

> 目标：把企业官方公开的校招岗位，稳定转换为“找岗位”可检索的数据。招聘类型只允许“全职”和“实习”。本文件是执行清单，不要求重写现有系统。

## 一、最终链路

```text
企业候选名录
  -> 官方招聘入口确认
  -> ATS/自建站识别
  -> 本地 fixture 和解析测试
  -> 单来源、低频、最多 20 条受控采集
  -> 列表/详情字段标准化
  -> 质量门禁
  -> jobs/upsert 或 job_quarantine
  -> /api/jobs
  -> 找岗位页面
```

企业官网入口不等于岗位。只有带具体岗位标题、地点、招聘类型、学历或要求、描述和明确详情/投递链接的记录，才允许进入活动岗位。

## 二、开源方案取舍

| 项目 | 用法 | 本项目结论 |
|---|---|---|
| [Scrapy](https://github.com/scrapy/scrapy) | 请求、限速、重试、Item/Pipeline | 参考设计；当前单来源 Worker 已足够，不迁移整套框架 |
| [Crawl4AI](https://github.com/unclecode/crawl4ai) | JS 页面内容提取 | 仅作为自建站的最后回退，不绕过风控 |
| [changedetection.io](https://github.com/dgtlmoon/changedetection.io) | 页面变化监控 | 参考快照/差异思想；当前用 listing hash 和 source snapshot |
| [JobSpy](https://github.com/speedyapply/JobSpy) | 第三方招聘站岗位发现 | 只做企业和入口线索，不作为官方岗位主数据 |
| [ATS Job Scraper](https://github.com/YvetteZheng0812/ats-job-scraper) | Greenhouse/Lever/Ashby/Workday 适配器思路 | 只借鉴 adapter 抽象；每个企业仍需确认官方归属 |

项目内已新增 `greenhouse` 和 `lever` 适配器：分别读取两类 ATS 的官方公开 jobs feed，要求注册表提供已核验的 board/site token，并要求每条记录自带具体详情/投递 URL、明确招聘类型和岗位内容。没有这些证据时只进入隔离，不会猜详情地址。

国内飞书、北森、Moka 等没有可直接覆盖所有企业的统一开源接口。实际方案是“ATS 通用适配器 + 企业专用适配器 + 明确的质量门禁”，而不是寻找一个万能爬虫。

## 三、数据标准

活动岗位必须具备：

| 前端字段 | 数据字段 | 规则 |
|---|---|---|
| 公司 | `company` | 企业标准名称 |
| 城市 | `city` | 多城市用 ` / `；没有证据填“未注明” |
| 职能 | `category` | 受控岗位族，不直接把长文本当分类 |
| 岗位 | `title` | 具体岗位名称，不能是招聘计划/活动/项目 |
| 招聘类型 | `job_nature` | 仅 `全职` 或 `实习` |
| 学历要求 | `degree` | 未注明/大专及以上/本科及以上/硕士及以上/博士 |
| 岗位描述 | `description` | 来源原文或确定性清洗结果 |
| 任职要求 | `requirements` | 来源原文或确定性清洗结果 |
| 官方投递 | `apply_url` | 具体岗位详情或投递地址，禁止猜 URL |

招聘类型识别规则：明确社会招聘、兼职、劳务、外包、实习生专项以外的类型，拒绝进入活动岗位；无法判断时隔离，不猜成全职。

## 四、每家企业的执行单元

每次只处理一个 `source_key`，按 S1-S8 顺序执行：

1. **S1 入口确认**：从企业官网导航进入招聘页，记录官方入口、最终招聘域名、ATS 类型和证据 URL。只登记 `candidate/analyzing`，不写岗位。
2. **S2 Fixture**：保存脱敏的 `tests/fixtures/<source-key>/listing.json` 和必要的详情样本。测试不得访问网络。
3. **S3 Adapter**：优先公开 JSON/API，其次服务端 HTML，再次 Playwright；适配器只返回标准中间对象，不直接写数据库，最多 20 条。
4. **S4 标准化测试**：覆盖城市、职能、学历、描述、要求、全职/实习、无详情 URL 拒绝、导航卡片拒绝。
5. **S5 受控真实样本**：只运行一次，串行，最多 20 条。遇到 403、429、验证码、安全验证、登录墙或详情链路不确定，立即停止并暂停来源。
6. **S6 入库与前端**：通过 runner/upsert 写入，确认 `/api/jobs` 可查询，找岗位页面自动出现，无需写死岗位卡片。
7. **S7 调度资格**：只有官方归属、字段门禁、详情/投递链接、真实样本全部通过，才设为 `confirmed/integrated`。
8. **S8 Harness**：记录访问次数、岗位数、接收数、隔离数、失败原因、保护动作、测试结果和下一步。

## 五、自动更新策略

- 列表未变化：只更新 `last_seen_at`，不重复请求详情。
- 新岗位、列表摘要变化或详情超过 7 天：才请求详情。
- 默认同一来源间隔 12 小时；近期有变化可缩短到 6 小时；连续失败使用 1/3/12/24 小时退避。
- 单来源串行、source lock、冷却保护；不使用代理池、指纹伪装或验证码绕过。
- 空快照或岗位数较上次下降超过 50%：快照保护，不下线现有岗位。
- 岗位连续 3 次完整健康快照缺失才标记 `inactive`；再次出现立即恢复。
- 详情失败不覆盖已有有效详情；不合格新观察写入 `job_quarantine`。

## 六、Luna 实际执行命令

在 `F:\牛投马面\V0.1\graduate-jobs-mvp` 执行：

```powershell
& .\.venv\Scripts\python.exe -m compileall -q app crawler tests
& .\.venv\Scripts\python.exe -m unittest discover -s tests -p 'test_*.py' -q
Invoke-WebRequest http://127.0.0.1:8000/health
Invoke-WebRequest 'http://127.0.0.1:8000/api/jobs?limit=20'
```

然后按一个来源执行：

```powershell
& .\.venv\Scripts\python.exe -m crawler.worker --source <source-key>
```

每个来源结束后必须再次运行编译、专项测试、全量测试、`/health`、`/api/jobs`，并追加 `EXECUTION_HARNESS_V03.md`。未通过时只修当前来源，不重构全项目，不删除历史岗位。

## 七、当前已完成与下一单元

### 本次更新：自建招聘站可见岗位卡片适配器已验收

- 新增 `crawler/adapters/custom_html.py`，仅解析公开页面渲染后可见、带明确岗位卡标识的岗位卡片。
- 导航卡片、招聘类型不明确、没有具体详情链接的记录拒绝进入活动岗位。
- 新增离线 fixture 和 `tests/test_custom_html_adapter.py`。
- 专项测试、全量测试、Python 编译和前端语法检查均通过；当前全量测试为 84/84。
- 对应执行说明见 `LUNA_JOB_INGESTION_RUNBOOK.md`。

### 本次更新：已有岗位来源与数据管理状态对账

- 修复小米、米哈游、叠纸、OPPO 已有有效岗位但仍显示为 `candidate/analyzing` 的状态不一致。
- 新增显式白名单迁移 `promote_verified_sources()`，仅允许有活动岗位且适配器匹配的来源升级为 `confirmed/reachable/integrated`。
- 当前 5 个已有岗位来源已进入调度闭环；未验证来源不会被升级。
- 手工 Worker 成功后会推进下一次调度时间，避免重复认为来源到期；调度器自身不会重复记账。
- 全量测试更新为 88/88。

### 本次更新：米哈游单来源受控更新

- 使用专用 `mihoyo` 适配器完成一次最多 20 条的低频更新。
- 结果：发现 20 条、更新 20 条、新增 0 条、下线 0 条、隔离 0 条。
- 成功记录来源的 `last_success_at` 与 `next_run_at`，岗位仍可在“找岗位”按公司检索。

### 本次更新：叠纸单来源受控更新

- 使用专用 `papegames` 适配器完成一次最多 20 条的低频更新。
- 结果：发现 19 条、更新 19 条、新增 0 条、下线 0 条、隔离 0 条。
- 已记录下一次调度时间；叠纸岗位仍可在“找岗位”按公司检索。

### 本次更新：小米全量刷新后的未来访问限额

- 小米本次历史刷新完成：发现 535 条、创建 73 条、更新 462 条，未下线或隔离岗位。
- 将后续自动更新改为最多 2 页、非完整快照，保留历史岗位，避免每次调度重复抓取全量列表。
- 全量抓取仅作为人工受控盘点，不作为默认自动调度行为。
- 当前全量活动岗位为 626 条，其中小米 575 条；4 条不合格观察已保留在隔离证据层。

### 本次更新：企业主数据去重

- 修复企业目录中的 `小米/小米集团`、`小鹏/小鹏集团`、`OPPO/oppo` 重复显示。
- 来源同步现在会先解析明确企业别名；一次性迁移将来源和岗位归并到标准企业名，原记录保留为 `merged`，不删除数据。
- 浏览器验证通过：小米、OPPO、小鹏各只显示一个标准企业入口。

### 本次更新：低频调度器运维脚本

- 新增 `start_scheduler.ps1` 与 `stop_scheduler.ps1`。
- 使用项目 PID 文件、命令行校验和隐藏后台进程，避免重复启动或误杀其他进程。
- 未绑定到 Web 服务自动启动；本轮在确认无到期来源后由操作员手动启动，并验证重复启动不会产生第二个进程。

### 本次更新：已有岗位来源与数据管理状态对账

- 修复小米、米哈游、叠纸、OPPO 已有有效岗位但仍显示为 `candidate/analyzing` 的状态不一致。
- 新增显式白名单迁移 `promote_verified_sources()`，仅允许有活动岗位且适配器匹配的来源升级为 `confirmed/reachable/integrated`。
- 当前 5 个已有岗位来源已进入调度闭环；未验证来源不会被升级。
- 全量测试更新为 87/87。

### 本次更新：保护性停止信号传递到调度器

- 修复 `crawler/runner.py` 未向调度器保留 `stop_reason` 的问题。
- `403/429/验证码/安全验证/登录墙` 现在会暂停来源并清空 `next_run_at`，普通超时仍使用退避。
- 新增调度器测试；当前全量测试为 86/86。

- 已接入具体岗位：小米、小鹏、叠纸、OPPO、米哈游。
- 当前活动岗位：553 条；企业 55 家；来源 64 个。
- 当前全量回归：79/79 通过。
- 本轮新增探查结果：百度、微软官方候选入口均因有界超时暂停；没有岗位写入，也没有重复重试。
- 本轮新增离线能力：Greenhouse 公共 ATS 适配器和 fixture，等待企业官方归属与 board token 证据后才能启用。
- 360、腾讯等来源仍因具体详情/投递 URL 证据不足暂停；北森字段别名已具备离线复用能力，但不代表来源已上线。
- 下一单元：从注册表选择一个“官方归属明确、公开详情 URL 明确”的候选来源，先完成 S1-S4；没有明确详情 URL 就只记录暂停原因，不做真实采集。

## 八、交付验收

- 找岗位每条卡片可显示公司、城市、职能、岗位、招聘类型、学历。
- 活动岗位招聘类型只有全职/实习。
- 岗位详情和官方投递链接可打开。
- 招聘计划、活动、导航卡片、空详情进入不了活动岗位。
- 同一来源岗位详情变化更新原记录，不产生重复。
- 失败、空快照、骤降不会污染或清空已有岗位。
- 每次真实访问都有 Harness 记录，403/429/验证码/登录墙后不自动重试。
