# Luna V0.2 开发执行规格

版本：2026-08-15

## 1. 唯一当前任务

在 `F:\牛投马面\V0.1\graduate-jobs-mvp` 中，按照 [PRODUCT_V0_2_IMPLEMENTATION_PLAN.md](PRODUCT_V0_2_IMPLEMENTATION_PLAN.md) 将现有 MVP 推进为 V0.2 可用产品。

历史 D-001 的“只做一个单元并停止”约束已失效。当前从 `UX-002` 开始，按以下顺序连续执行：

```text
UX-002 → UX-003 → P-003 → M-005 → P-004 → D-006 → OPS-001 → R-002
```

每完成一个最小单元必须自动测试并写入 Harness。若验收通过且没有外部卡口，可以继续；不要为了等待常规确认而停止。P-004 缺少真实模型配置时标记为被阻塞并跳到 D-006，但不得伪造模型结果。

## 2. 开始前必须完整阅读

1. `PRODUCT_V0_2_IMPLEMENTATION_PLAN.md`
2. `DEVELOPMENT_OUTLINE.md`
3. `GATE_ANALYSIS.md`
4. `EXECUTION_HARNESS.md`
5. `EVALUATION_CASES.md`
6. `USAGE_GUIDE.md`
7. 适用的 `AGENTS.md`

冲突优先级：用户最新指令 > 本文件 > V0.2 实施方案 > 其他历史文档。Harness 中历史记录只用于追溯，不是当前范围限制。

## 3. 不得改变的产品决策

1. 原始简历默认不保存，只保存用户确认后的结构化画像。
2. 能力证据与求职意愿分离；简历不能替用户决定岗位、企业、城市和排除项。
3. 全部岗位检索始终可用；AI 只排序、解释和发现机会。
4. 默认推荐比例为 50% 主攻、25% 目标企业、20% 相邻、5% 探索。
5. 竞争风险只显示高/中/低和依据，不显示录取概率。
6. 销售岗位不能因为数量多获得推荐优势。
7. 只做校招、应届招聘和校招体系内实习。
8. 不自动投递，只跳转官方入口。
9. 403、429、验证码、登录墙、安全验证立即停止，不绕过、不重试。
10. 远景 6 条非岗位记录保持隔离；360/北森冷却窗口内不重复访问。

## 4. 开发纪律

- 保留现有 API 兼容性；需要分页时可以扩展返回结构，但必须提供旧调用兼容或同步迁移全部调用方和测试。
- 数据库迁移只允许非破坏性 `CREATE TABLE IF NOT EXISTS`、`ALTER TABLE` 和显式数据回填。
- 不删除失败运行、隔离证据、原始压缩包或用户现有数据。
- 修改前记录数据库统计和指纹，修改后复核。
- 每个新适配器必须先有本地 fixture 与测试，再做单来源低频小样本。
- 任何页面都不能在加载时触发招聘官网访问。
- API 密钥只从环境变量读取，禁止写入代码、前端、日志、测试夹具和 Markdown。
- 不顺手实现 V1 的账号、消息推送、自动投递和向量数据库。

## 5. 当前第一单元 UX-002

立即实施前后台拆分：

- `/`：仅岗位检索、推荐、岗位详情和反馈状态。
- `/profile`：简历预览、分析确认、能力证据和求职意愿。
- `/admin/sources`：企业注册表、质量、接入队列、采集运行和岗位质量。

优先复用现有 HTML、CSS、JavaScript 和 API，先迁移再清理。不要在拆页单元重写后端业务或触发真实采集。

UX-002 验收后继续 UX-003，详细更新清单和验收标准以 V0.2 实施方案第 7 节为准。

## 6. 每个 Harness 记录模板

```text
### RUN-<UNIT>-<SEQ> / <名称>

- 日期：
- 状态：Completed / Failed / Blocked
- 开始基线：岗位总数、活动数、隔离数、企业数、入口数、运行数、测试数
- 范围：
- 修改文件：
- 数据迁移：无 / 内容与回滚说明
- 自动验证：
- 浏览器验证：
- 外部访问：无 / 精确来源与停止条件
- 数据结束值：
- 问题与处理：
- 结论与下一单元：
```

失败记录不得覆盖。发生高风险失败时停止当前单元并保护已有数据；只要后续单元不依赖该失败，可以记录阻塞后继续。

## 7. 最终交付

完成或遇到真正外部阻塞后，向用户汇报：

- 已完成和未完成的单元；
- 三个页面的可访问地址；
- 数据与测试最终基线；
- 新增或修改的文件；
- 真实采集和真实 AI 是否发生；
- 被阻塞项需要用户提供什么；
- 下一步最小动作。

## 8. 可直接交给 Luna 的指令

```text
请在 F:\牛投马面\V0.1\graduate-jobs-mvp 中继续开发校招岗位智能工作台 V0.2。

开始前完整阅读 LUNA_IMPLEMENTATION_SPEC.md、PRODUCT_V0_2_IMPLEMENTATION_PLAN.md、DEVELOPMENT_OUTLINE.md、GATE_ANALYSIS.md、EXECUTION_HARNESS.md、EVALUATION_CASES.md、USAGE_GUIDE.md 和适用的 AGENTS.md。

以 LUNA_IMPLEMENTATION_SPEC.md 为当前执行规格，从 UX-002 开始，依次执行 UX-003、P-003、M-005、P-004、D-006、OPS-001、R-002。每完成一个单元都运行自动测试和浏览器回归并追加 Harness；没有卡口失败时继续，不要逐项等待确认。缺少真实 Luna 配置时不得伪造验收，将 P-004 标记阻塞后继续不依赖它的工作。任何真实招聘来源一次只验证一家，遵守冷却，403/429/验证码/登录墙立即停止，不绕过、不重试。不得破坏现有有效岗位、隔离证据和历史运行记录。
```
