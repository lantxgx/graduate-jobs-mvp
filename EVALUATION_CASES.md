# 推荐离线验收集

这组案例对应产品卡口中的高风险场景。它们使用合成岗位，不调用外部模型或招聘官网，作为规则引擎回归集。当前由 30 个可执行变体组成（每类 5 个）：

1. 算法能力画像面对大量销售岗位：销售不能因为数量多进入主攻池。
2. 用户主动指定简历中没有出现的目标企业：目标企业岗位必须被单独召回。
3. 主攻算法岗位竞争风险高：仍保留在主攻池，风险只作为独立解释维度。
4. 简历缺少毕业年份：不能据此硬过滤岗位。
5. 用户明确排除销售：销售从推荐池排除，但仍保留在全部岗位检索。
6. 用户点击忽略：只改变默认推荐，不改变完整岗位集合。

## 30 个执行案例

| 类别 | 案例编号 | 验证内容 |
|---|---|---|
| 销售数量偏置 | sales-volume-0～4 | 算法主攻岗位不会被 8 条销售岗位挤出主攻池 |
| 目标企业召回 | target-company-0～4 | 简历未出现的目标企业仍进入独立目标企业池 |
| 竞争风险 | competition-0～4 | 算法岗位风险为 high，但仍保留 direct 主攻匹配 |
| 年份缺失 | missing-year-0～4 | 缺少毕业年份不会被错误硬过滤 |
| 明确排除 | sales-exclusion-0～4 | 销售仍能在全部岗位中查到，但不进入推荐 |
| 解释完整性 | explanation-0～4 | 推荐保留多通道召回和六个匹配维度 |

可执行证据：`tests/test_recommendation_eval.py`，与主测试套件一起运行。该集合不生成录取概率，也不把简历证据自动改写为求职意愿。
