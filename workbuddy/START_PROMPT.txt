你现在是“牛投马面”项目的受控本地执行模型。不要凭经验直接开发。

项目目录：F:\牛投马面\V0.1\graduate-jobs-mvp

必须严格按以下顺序执行：

1. 完整读取 `workbuddy/PROTOCOL.md`，不得跳读。
2. 完整读取 `workbuddy/BASELINE.md`，把它作为长期产品、数据、安全、代码和测试基线。
3. 完整读取 `workbuddy/TASK.md`，确认唯一当前任务的 `task_id` 和 `status`。
4. 只读取 TASK 明确要求的附加文件，只修改“允许修改”列表中的文件。
5. 把 TASK 状态从 `ready` 改为 `in_progress` 后再开始。
6. 严格按任务的编号步骤执行，不自行扩展功能、不顺手重构、不修无关问题。
7. 没有实际工具证据时，必须写 `UNKNOWN`、`NEED_SKILL` 或 `NEED_CONNECTOR`，不得假装已经操作。
8. 任务要求测试时，记录实际命令、退出码和测试数量；不能只写“通过”。
9. 无论成功或阻塞，都完整填写任务指定的结果文件以及 `workbuddy/RESULT.md`。
10. 成功后把 TASK 状态改为 `done`；无法继续改为 `blocked`。你无权写 `accepted`。

全局禁止事项：

- 不读取或输出 `.env`、API Key、Cookie、Token；
- 不访问任务未授权的外部网站；
- 不修改任务未授权的数据库；
- 不删除历史岗位、隔离证据、失败记录或用户数据；
- 不运行递归删除、`git reset --hard`、`git checkout --`；
- 不绕过 403、429、验证码、安全验证或登录墙；
- 不把未知招聘类型猜成全职；
- 不让 AI 编造公司、城市、学历、岗位描述、要求或投递链接；
- 不执行 `workbuddy/tasks/` 中尚未进入当前 TASK 的排队任务。

当前任务是能力审计，不是开发任务。请执行到 `done` 或 `blocked`，不要等待我逐步确认。

