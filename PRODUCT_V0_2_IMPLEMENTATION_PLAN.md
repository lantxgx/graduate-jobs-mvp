# 校招岗位智能工作台 V0.2：完整实施方案

版本：2026-08-15  
实施对象：Luna 开发模型  
当前基线（2026-08-15）：553 条活动岗位、55 家企业、64 条招聘入口、0 条岗位隔离观察、78/78 测试通过；历史 Harness 中的 520/507 等数字保留为当时执行单元的基线，不代表当前数据库。

## 1. 产品要解决的问题

产品不是单纯的招聘网站聚合搜索，而是帮助校招生完成一条完整决策链：

```text
完整岗位发现
→ 建立能力证据与求职意愿
→ AI/规则辅助排序和解释
→ 主攻、目标企业、相邻、探索机会组合
→ 收藏、比较、投递和进度管理
→ 始终跳转企业官方入口投递
```

常规检索负责“不漏”，推荐负责“降低筛选成本”，用户主动设置负责纠正 AI 对意愿的误判。AI 不拥有岗位的最终过滤权。

## 2. V0.2 产品边界

### 本版本必须完成

- 用户首页只展示岗位搜索、筛选、推荐和投递状态，不再混入采集后台。
- 独立画像页负责简历预览、结构化分析、用户确认和求职意愿设置。
- 独立管理页负责企业入口、采集质量、异常来源、运行记录和岗位质量。
- 全部 553 条活动岗位都可被分页访问，不能只展示前 100 条。
- 岗位详情完整显示地点、招聘类型、岗位描述、任职要求、来源和官方投递链接。
- 推荐继续采用四类机会池，并展示匹配依据、缺失证据和竞争风险。
- 收藏、不感兴趣、已投递可恢复；不感兴趣不影响全部岗位搜索。
- 原始简历默认不保存；AI 结构化结果必须经用户确认。
- 只收录校招、应届招聘和校招体系内实习，不自动投递。

### 本版本不承诺

- 不承诺覆盖所有企业，也不以企业入口数量代替具体岗位数量。
- 不给出录取概率，不承诺 AI 推荐绝对准确。
- 不绕过登录、验证码、403、429 或反自动化限制。
- V0.2 保持本地单用户模式；账号、多设备同步、消息提醒放到 V1。
- 未提供真实 Luna/OpenAI-compatible 配置前，不伪造外部模型验收结果。

## 3. 信息架构

| 路径 | 面向对象 | 核心内容 |
|---|---|---|
| `/` | 求职用户 | 全部岗位、智能推荐、筛选、详情、收藏和投递状态 |
| `/profile` | 求职用户 | 简历处理、能力证据、求职意愿、推荐组合设置 |
| `/admin/sources` | 维护人员 | 企业入口、ATS、质量等级、接入队列、采集运行和岗位质量 |

全站顶部导航只保留“找岗位”“我的画像”“数据管理”。用户首页可以显示一句数据更新时间和质量摘要，但不能显示企业注册表明细。

## 4. 核心交互

### 4.1 找岗位

- 默认进入“全部岗位”，不要求先上传简历。
- 支持关键词、企业、城市、岗位族、招聘类型、学历筛选。
- 结果区提供“全部岗位、为我推荐、已收藏、已投递”四个主视图；不感兴趣放到二级管理入口。
- 岗位卡只展示决策所需摘要；点击后打开详情抽屉或详情区。
- 每个岗位显示信息完整度和最近发现时间，官方页面是最终事实来源。
- 推荐视图按“主攻、目标企业、相邻、探索”分组，不混成一个不可解释总榜。
- 没有画像时，推荐按钮引导去 `/profile`，但全部岗位搜索照常可用。

### 4.2 我的画像

页面采用四步流程，而不是把所有输入框平铺：

1. 上传 PDF/DOCX 并预览，仅当前请求处理。
2. 可选调用结构化分析，展示候选值、不确定项和证据。
3. 用户确认后，将学历、毕业年份、专业、技能写入“能力证据”。
4. 用户主动填写主攻岗位、相邻岗位、目标企业、城市和排除项。

能力证据与求职意愿必须分区显示。保存前提供摘要；简历不得自动填写目标岗位、目标企业或排除方向。

### 4.3 数据管理

- 概览：企业数、入口数、活动岗位数、隔离数、字段缺失数、最近运行状态。
- 企业来源：按 ATS、官方性、可访问性、质量和优先级筛选。
- 接入队列：显示“为什么排在这里”和下一动作，不在列表加载时自动采集。
- 运行记录：成功、失败、保护停止原因和冷却剩余时间。
- 岗位质量：异常记录可审计，不直接删除原始证据。
- 手动采集必须有二次确认，仅允许单来源；遇停止条件立即结束。

## 5. 匹配与推荐规则

### 5.1 召回

候选集由关键词、岗位族、能力标签、目标企业和相邻岗位图谱共同合并。目标企业中满足基本资格的岗位必须被评估，不能因简历未出现企业名而漏掉。

### 5.2 硬条件与软条件

- 只有用户明确确认的排除岗位、届别、学历等可以构成硬条件。
- 技能相似、专业相关、竞争风险和反馈只影响排序。
- 地点默认按偏好处理；只有用户显式选择“仅这些城市”时才作为硬条件。

### 5.3 四类推荐池

- 50% 主攻岗位：与主动目标和能力证据高度一致。
- 25% 目标企业：确保想投企业的可用机会被看见。
- 20% 相邻岗位：说明共享技能、缺失技能和转型距离。
- 5% 探索岗位：低频发现新方向，不被销售类岗位数量淹没。

销售岗位只有在用户明确选择销售，或有能力证据且未排除时才参与相关池；岗位数量本身不产生加分。

### 5.4 推荐解释

每条推荐至少返回：基本资格、能力匹配、求职意愿、企业偏好、转型距离、证据置信度、缺失证据和竞争风险依据。竞争风险仅为高/中/低。

## 6. 数据与采集策略

### 来源状态机

```text
候选入口
→ 官方归属确认
→ 公开访问与合规检查
→ ATS/接口识别
→ 1 家 1 次小样本
→ 具体岗位质量门槛
→ 快照保护验收
→ 才允许进入低频更新
```

### 访问约束

- 同一来源默认至少冷却 3600 秒，正式调度建议按 6–12 小时更新。
- 来源串行访问并带随机抖动；不通过提高并发、代理轮换、指纹伪装规避限制。
- 403、429、验证码、安全验证、登录墙立即停止，不重试、不绕过。
- 优先使用企业公开职位接口或官方 ATS 数据；浏览器发现只用于首次识别。
- 使用 ETag、Last-Modified、内容哈希和快照对比减少无效请求。
- 一次消失不下线岗位，连续 2–3 个完整健康快照未出现后再失效。

### 扩展顺序

先扩展已经确认官方、公开可访问且 ATS 可复用的来源。每次只接一家企业，先取得 5–20 条具体岗位样本，再决定是否进入稳定更新。远景保持隔离，360/北森在冷却期内不重复访问。

## 7. 实施单元与顺序

Luna 必须按以下顺序执行。每个单元完成后自动验收、写 Harness；如果没有卡口失败，可继续下一个单元，不需要等待用户逐项确认。

### UX-002：前后台与画像页面拆分（P0）

目标：解决当前三个区域割裂和职责混乱。

更新：

- `app/main.py`：增加 `/profile`、`/admin/sources` 静态页面路由。
- `static/index.html`：只保留岗位工作台。
- 新增 `static/profile.html`、`static/admin-sources.html`。
- 拆分前端脚本为共享 API/工具、岗位页、画像页和管理页；迁移现有功能，不删除 API。
- `static/styles.css`：增加统一导航、页面容器、状态和移动端样式。
- `tests/`：增加三路由和静态资源烟雾测试。

验收：三个 URL 均为 200；首页不再出现企业入口表和简历输入框；画像功能与管理统计均可用；31 个原测试不回归。

### UX-003：完整岗位分页与详情体验（P0）

目标：确保“全部岗位”真的完整可访问，并能看清具体岗位信息。

更新：

- `app/db.py`、`app/main.py`：岗位查询增加稳定分页参数、总数和分页元数据，保持旧参数兼容。
- `static/jobs.js`：分页或“加载更多”、URL 查询条件同步、加载/空/错误状态。
- 岗位详情展示地点、招聘类型、学历、岗位族、描述、要求、发布时间、最近发现和官方链接。
- 增加分页、筛选组合、详情字段和 XSS 文本渲染测试。

验收：无筛选时可逐页访问全部 553 条活动岗位；翻页无重复/遗漏；详情关键字段完整；隔离记录不进入用户列表。

### P-003：画像引导与确认闭环（P0）

目标：让用户知道哪些由简历产生、哪些必须自己决定。

更新：

- 画像页改为四步流程，显示本地规则/外部 AI provider 状态。
- 分析结果逐字段确认，不提供“一键替用户决定意愿”。
- 地点增加“偏好/硬限制”开关；推荐比例允许调整且总和校验为 100%。
- 数据结构迁移必须非破坏，旧画像自动使用默认值。

验收：不上传简历也可手填；未确认结果不保存；原文件不落库；意愿字段不会被简历分析覆盖；删除画像后推荐回到未配置状态。

### M-005：推荐离线评测与排序校准（P0）

目标：先证明当前规则不会产生高风险错误，再考虑语义模型。

更新：

- 扩展 `EVALUATION_CASES.md` 和 `tests/test_recommendation_eval.py`，至少形成 30 个脱敏/合成案例。
- 指标包括：硬条件误杀率、主攻召回、目标企业覆盖、无关销售 Top-N 占比、相邻岗位解释完整率、推荐可解释率。
- 建立可重复执行的评测命令和 JSON/Markdown 报告。
- 根据失败案例仅调整可解释规则，不直接引入黑盒综合分。

发布卡口：硬条件误杀为 0；目标企业覆盖 100%；明确排除销售时推荐销售为 0；推荐解释完整率 100%。

### P-004：真实 Luna 结构化分析验收（P1，外部配置卡口）

目标：将真实模型只用于简历候选字段提取和解释增强。

前提：用户提供兼容端点、模型名和密钥，并明确同意外发脱敏样本。

更新：

- 使用现有 `AI_PROFILE_*` 配置，不把密钥写入代码、日志或 Harness。
- 采用 JSON Schema 校验输出；超时、格式错误、配置缺失时失败关闭并允许回到本地规则。
- 用 10 份脱敏/合成简历验收，不使用真实个人简历作为自动测试夹具。

验收：关键字段均带证据和置信状态；模型不能生成求职意愿；失败不覆盖已有画像。没有配置时将此单元记为“被阻塞”，继续执行不依赖它的单元。

### D-006：真实企业岗位扩展流水线（P1）

目标：从“入口多”转向“有效具体岗位多”。

执行方式：每个来源独立子运行 `RUN-D006-企业名-序号`，一次只做一个来源。

更新：

- 优先复用飞书、北森、Moka 适配器，再为自建系统增加独立适配器。
- 每个新来源增加本地脱敏 fixture、解析测试、质量统计和快照保护测试。
- 只有小样本通过 G-JOB-01～05 后才能写入活动岗位。

验收：每家来源都有官方证据、明确岗位 ID/详情链接、必需字段完整率 100%、无导航卡片误入；失败来源不污染岗位库。

### OPS-001：可控更新与可观测性（P1）

目标：让数据可持续更新，同时降低误抓和异常覆盖风险。

更新：

- 将采集从 Web 请求同步执行迁移为独立 CLI/Worker 任务；管理页只创建单来源任务或显示命令，不长时间阻塞 API。
- 增加每来源更新频率、最近成功、连续失败、冷却、快照数量变化和保护原因。
- 默认关闭未通过验收的自动更新；失败退避，不自动绕过访问控制。

验收：API 服务重启不触发采集；一个来源失败不影响其他来源；重复任务受锁和冷却保护；运行记录可在管理页查看。

### R-002：V0.2 发布验收（P0）

目标：对产品而非单个接口做完整回归。

验收路径：搜索岗位 → 查看详情 → 保存画像 → 获取四类推荐 → 收藏 → 标记已投递 → 在全部岗位中找到被忽略岗位 → 查看数据管理质量。

必须通过：全部自动测试、JS/Python 语法检查、数据库基线审计、三个页面浏览器回归、控制台无错误、窄屏基础可用、README 和使用说明同步。

## 8. 暂缓到 V1 的工作

- 用户注册登录、跨设备同步和隐私数据删除审计。
- SQLite 迁移 PostgreSQL、任务队列和正式部署。
- 邮件/微信提醒、截止日期订阅和消息推送。
- 基于评测证据的语义召回或向量数据库。
- 岗位对比、投递日历和面试复盘。

这些能力不能插队阻塞 V0.2 的完整岗位访问、数据质量和推荐可靠性。

## 9. 文档与状态同步要求

每个实施单元必须同步：

- `EXECUTION_HARNESS.md`：状态、改动、测试、数据前后值、问题和结论。
- `README.md`：当前可用功能、启动方式、最终路由和 API。
- `USAGE_GUIDE.md`：用户可见操作发生变化时更新。
- `DEVELOPMENT_OUTLINE.md`：只维护当前里程碑状态，不重复堆叠运行细节。
- 新增迁移、环境变量或脚本时同步 `.env.example` 和命令说明。

禁止覆盖失败历史；过时数字必须更新，不能让同一文件同时声称 513 和 507 条活动岗位。

## 10. 完成定义

V0.2 完成不是“页面能打开”，而是同时满足：

1. 用户无需理解采集后台即可搜索并查看全部具体岗位。
2. 用户能分清能力证据和求职意愿，并能纠正 AI。
3. 推荐不会取代完整检索，不会被销售类数量偏置。
4. 每条推荐和竞争风险都可解释。
5. 新企业数据只有通过官方性、合规、具体岗位和快照卡口才进入岗位库。
6. 失败采集、外部 AI 失败和异常快照不会破坏已有有效结果。
7. 全链路自动测试和浏览器回归有 Harness 证据。
